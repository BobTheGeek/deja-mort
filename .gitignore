# Godot 4
.godot/
/android/
*.translation

# Exports. The presets are committed so a build is reproducible; they hold no
# secrets — keystore path and password come from GODOT_ANDROID_KEYSTORE_DEBUG_*
# at export time.
/build/

# Paid asset packs are never committed. Document them in assets/README.md.
assets/paid/

# gdUnit4 reports
reports/

# OS
.DS_Store
Thumbs.db
